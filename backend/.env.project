# Backend environment variables (example)
PORT=4000

# Database: for dev we will use SQLite file in backend (example)
# e.g. DATABASE_URL="file:./dev.db"
DATABASE_URL=

# LLM / Provider secrets (server-side only). Do NOT put these in frontend.
# Example variable names (leave blank in this example file)
OPENAI_API_KEY=
AZURE_OPENAI_KEY=
ANOTHER_LLM_API_KEY=

# Admin / internal tokens (server-side only)
ADMIN_API_TOKEN=

# Note: DO NOT prefix server-side secrets with VITE_ or PUBLIC_. Keep secrets server-side.
