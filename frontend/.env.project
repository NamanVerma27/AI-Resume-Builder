# Frontend public/example env (do NOT put secrets here)
# Vite requires public vars to be prefixed with VITE_ if you want them baked into the build.
VITE_API_BASE_URL=http://localhost:4000

# Example flag to select mock provider in dev (non-secret)
VITE_LLM_PROVIDER=mock
